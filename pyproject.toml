[project]
name = "hugin-munin-error-labelling"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Marie Roald", email = "marie.roald@nb.no"},
]
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=2.2.2",
    "datasets>=2.21.0",
    "pillow>=10.4.0",
    "stringalign @ git+https://github.com/yngvem/stringalign.git",
]
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}

[tool.pdm]
distribution = false
